# AGENTS.md

## コミットメッセージ規約

- AIエージェントがコミットを作成する場合、次のルールに従う。
  - コミットメッセージは日本語で記述する。
  - コミットメッセージは Conventional Commits の仕様に準拠するよう努める。
  - コミットメッセージのdescriptionの冒頭に「[AI]」接頭辞を追加する。
  - コミットメッセージの最後に `Assisted-by:` を含める。
  - `Assisted-by:` には、実行したAIエージェント自身のモデル名を含める。
- t_wada の提唱に従い、コミットメッセージには Why を含める。

## Git運用ルール

- AIエージェントは、ローカル環境での変更作業とコミット作成まで実施してよい。
- `git pull`、`git push`、`git fetch` など、リモートリポジトリとの通信を伴う操作は人間のみが実施すること。
- 大きな変更をする際は事前にブランチを作り、各作業の結果をそのブランチでコミットしながら進める。マージは人間が行う。

## Dev Container 共通ルール

### 基本方針

- この環境は比較的安全な開発環境であるため、AIエージェントはシェルコマンドの実行を許可される。
- ただし高い権限は一般に付与されていないため、権限を要する操作は失敗する可能性がある。

### 実行ポリシー

- 通常の開発コマンド（例: `npm test`, `git` など）は、原則として追加確認なしで実行してよい。
- Dockerコンテナ操作が必要な場合は、AIエージェントが開発者に実行を依頼する。

### 権限不足・実行失敗時の対応

- 権限不足や環境制約でコマンド実行に失敗した場合、AIエージェントは開発者に実行を依頼する。
- 依頼時は毎回、次の3点を必ず提示する。
  1. 実行しようとしたコマンド
  2. そのコマンドの目的（なぜ必要か）
  3. 開発者向けの実行手順
